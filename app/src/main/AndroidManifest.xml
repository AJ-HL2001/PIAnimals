<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.studium.pianimals">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PIAnimals"
        android:usesCleartextTraffic="true">
        <activity android:name=".ListaVacunas"></activity>
        <activity android:name=".DetallesNota" />
        <activity android:name=".ListaNotas" />
        <activity android:name=".NuevaMascota" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".articulos.Art1" />
        <activity android:name=".articulos.Art14" />
        <activity android:name=".articulos.Art13" />
        <activity android:name=".articulos.Art12" />
        <activity android:name=".articulos.Art11" />
        <activity android:name=".articulos.Art10" />
        <activity android:name=".articulos.Art9" />
        <activity android:name=".articulos.Art8" />
        <activity android:name=".articulos.Art7" />
        <activity android:name=".articulos.Art6" />
        <activity android:name=".articulos.Art5" />
        <activity android:name=".articulos.Art4" />
        <activity android:name=".articulos.Art3" />
        <activity android:name=".articulos.Art2" />
        <activity android:name=".MasInfoPet" />
        <activity
            android:name=".splash_activity"
            android:label="@string/app_name"
            android:theme="@style/Theme.PIAnimals.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.PIAnimals.NoActionBar" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <uses-library android:name="org.apache.http.legacy" android:required="false"/>
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="@string/google_maps_key"/>
    </application>

</manifest>